import fs from 'node:fs/promises';
const out=new URL('../outputs/',import.meta.url),read=f=>fs.readFile(new URL(f,out),'utf8');
let h=await read('quality/console-assembly.html');
const three=await read('source/three.mjs'),model=await read('source/center-console.mjs'),hingeModel=await read('source/console-hinge-fixings.mjs'),hingeFinish=await read('source/hinge-lookdev.mjs');
h=h.replace("import * as T from '../source/three.mjs';import {buildGT01Console} from '../source/center-console.mjs';",`import * as T from 'data:text/javascript;base64,${Buffer.from(three).toString('base64')}';\n${model}\n${hingeModel}\n${hingeFinish}`);
h=h.replace('const assembly=buildGT01Console(T);',`const assembly=completeGT01ConsoleHinges(T,buildGT01Console(T));assembly.definition.assemblySteps.push({title:'鉸鏈固定與插銷止退',parts:[...new Set(assembly.parts.filter(p=>p.userData.id.startsWith('I03-HF-')).map(p=>p.userData.subPart))],direction:'固定側向下、活動側向上；端帽沿鉸鏈軸裝入。',note:'四組固定件、兩軸雙端套筒與端帽；詳細剖面、分件參考和接收形體請開啟鉸鏈專頁。'});`);
h=h.replace('<meta charset="utf-8">','<meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">');
h=h.replaceAll('../references/','references/').replace("src='../'+d.reference","src=d.reference");
h=h.replaceAll('I03-assembly-r1.png','I03-assembly-r4.png');
h=h.replace('<header>','<p style="padding:16px 24px 0">יהוה</p><header>');
h=h.replace('<button id="context">座艙回裝</button>','<a href="GT01-atelier.html?partRef=I03">查看整車接合</a>');
h=h.replace('<select id="part">','<select id="assemblyStep" aria-label="逐步組裝"></select><select id="part">');
h=h.replace('<button id="lid">','<a href="GT01-console-hinge-assembly.html">鉸鏈逐件／接合剖面</a><button id="lid">');
h=h.replace('<button id="lid">開／關扶手</button>','<button id="lid">開／關扶手</button><button id="knobAssembled">旋鈕三層接合</button><button id="knobExploded">拆開旋鈕三層</button>');
h=h.replace('<style>','<style>*{box-sizing:border-box}');
h=h.replace('</style>',`a{color:#2c5b47;text-underline-offset:4px;margin:0 6px}header>div{min-width:0}select{width:100%;max-width:410px}aside img{cursor:zoom-in}.teaching{max-width:1250px;margin:40px auto;padding:0 24px;line-height:1.85}.teaching h1{font:500 32px Georgia,'Microsoft JhengHei',serif}.teaching p{font-size:16px}.teaching table{width:100%;border-collapse:collapse;font-size:13px}.teaching th,.teaching td{padding:12px;text-align:left;vertical-align:top;border-bottom:1px solid #c3cabc}.table-wrap{overflow:auto}.notice{background:#eee6d7;padding:20px;border-left:3px solid #a58a50}dialog{border:0;max-width:95vw;max-height:94vh}dialog img{max-width:90vw;max-height:83vh}dialog::backdrop{background:#17271dde}@media(max-width:760px){header{display:block}header>div+div{margin-top:20px}main{grid-template-columns:minmax(0,1fr);height:auto}#stage{height:450px}aside{overflow:visible;padding:20px}select{max-width:100%}.teaching{padding:0 20px}}</style>`);
const lessons=`<section class="teaching"><nav><a href="GT01-atelier.html">車與工廠</a> · <a href="GT01-dashboard-assembly.html">儀表台與出風口</a> · <a href="GT01-instrument-assembly.html">駕駛儀表</a> · <a href="GT01-car-feedback.html">實際缺陷與改法</a></nav><h1>一個鞍座，從包覆到真正的空腔。</h1><p>先選上方的組裝階段，再選單件：每件都有獨立生成參考、用途、接收件與裝入方向。六個角度查看輪廓；分層按鈕用來辨認材料，不把黏合皮革當成可任意拆開的維修件。</p><p class="notice">目前是作者重建的形體教材。雙杯架、置物腔和隱藏接合是依用途補充；原車參考不可見的內容另行標示。固定螺栓、扣鎖釋放、編碼器與線束仍需施工，不能把這個可開蓋模型當作已完成的實體機構。</p><h2>五個接合問題，如何真正檢查</h2><ol><li><b>控制軸不能只對中心。</b>面板繞 Y 轉 −0.12 弧度，孔軸與 D 軸共用 n = [−sin(0.12), 0, cos(0.12)]。裝入方向是 −n；旋鈕孔、套和帽也要查外徑與頂端。</li><li><b>柔性內襯也有厚度。</b>容器內半徑減去內襯外半徑，才是同高度的徑向間隙。凸點要納入；第一版只看基本圓壁，忽略外擴凸緣，已退修。</li><li><b>空腔底部必須存在。</b>從杯口與置物口沿 −Z 射入實際網格，應先讀到內襯底面。中心開口不能由視覺陰影掩蓋。</li><li><b>鉸鏈需要共同軸。</b>扶手上一點的開啟位置 P′ = C + R<sub>Y</sub>(θ)(P−C)，C=[430,0,452] mm。固定葉保持不動、活動葉與蓋同轉；這個關係可套用箱蓋和櫃門，仍要另外驗證整段路徑及止退。</li><li><b>回裝要讀目前的車。</b>底板在 z=311.15 mm；既有地毯上面是 311 mm。0.15 mm 是目前形體的名義間距，不是施工公差或壓縮量。座椅側向間隙另從真實剖面讀取。</li></ol><h2>每種形體的用途與接收面</h2><div class="table-wrap"><table><thead><tr><th>分件</th><th>用途</th><th>接收件</th><th>組合關係</th></tr></thead><tbody id="partLessons"></tbody></table></div><p><a href="quality/console-parent-R4.md">施工交接</a> · <a href="quality/console-native-r4.json">原生形體讀回</a> · <a href="references/I03-reference-adoption.json">參考採用與退回</a></p></section><dialog id="imageZoom"><button id="zoomClose">關閉 ×</button><img alt="中央鞍座零件參考放大"></dialog>`;
h=h.replace('<script type="module">',lessons+'<script type="module">');
// The formal page is portable. Whole-car context stays in the canonical atelier.
const begin=h.indexOf('let contextRoot=null;'),end=h.indexOf('window.CONSOLE={',begin);
if(begin<0||end<0)throw Error('Console reader contract changed');
h=h.slice(0,begin)+`let contextRoot=null;function lid(open=hinge.rotation.y===0){hinge.rotation.y=open?.96:0;assembly.root.updateMatrixWorld(true);render();}document.querySelector('#lid').onclick=()=>lid();
let currentStep=6;const stepSelect=document.querySelector('#assemblyStep');assembly.definition.assemblySteps.forEach((s,i)=>{const o=document.createElement('option');o.value=i+1;o.textContent=(i+1)+' / '+s.title;stepSelect.append(o);});stepSelect.value='6';
function showKnob(exploded=false){const bond=assembly.parts.find(p=>p.userData.subPart==='I03-P23'),cap=assembly.parts.find(p=>p.userData.id===bond.userData.receiving.instanceId),insert=assembly.parts.find(p=>p.userData.receiving.instanceId===bond.userData.id),items=[cap,bond,insert];selected='旋鈕三層接合';layers('assembled');const n=new T.Vector3(-Math.sin(.12),0,Math.cos(.12));for(const p of assembly.parts)p.visible=items.includes(p);if(exploded){bond.position.addScaledVector(n,14);insert.position.addScaledVector(n,28);}document.querySelector('#partRef').src='references/I03-P23-reference-r3.png';document.querySelector('#refTitle').textContent='金屬帽 → 接合層 → 黑色嵌片';document.querySelector('#info').textContent='局部面板座標 mm：金屬承托面 Z16.50；接合層 Z16.50–16.70；黑色嵌片 Z16.70–17.20。外框內徑23.8，嵌片23.4，徑向名義间隙(23.8−23.4)/2=0.2。展示分層不代表已驗證貼合工序；黏著劑材料、固化與剝離性能未驗。'.replace('间','間');assembly.root.updateMatrixWorld(true);view('iso');}
document.querySelector('#knobAssembled').onclick=()=>showKnob(false);document.querySelector('#knobExploded').onclick=()=>showKnob(true);
function step(n){currentStep=n;selected='all';partSelector.value='all';hinge.rotation.y=0;layers('assembled');const ids=new Set(assembly.definition.assemblySteps.slice(0,n).flatMap(s=>s.parts));for(const p of assembly.parts)p.visible=ids.has(p.userData.subPart);stepSelect.value=String(n);view('iso');}
stepSelect.onchange=()=>step(Number(stepSelect.value));
new ResizeObserver(()=>{renderer.setSize(stage.clientWidth,stage.clientHeight);camera.aspect=stage.clientWidth/stage.clientHeight;view(viewName);}).observe(stage);
const rows=document.querySelector('#partLessons');for(const p of assembly.parts){const d=p.userData,tr=document.createElement('tr');for(const v of [d.id+' · '+p.name,d.purpose,d.receiving.ids?.join(', ')||d.receiving.instanceId||d.receiving.subPart,d.receiving.relationship]){const td=document.createElement('td');td.textContent=v;tr.append(td);}rows.append(tr);}
const zoom=document.querySelector('#imageZoom');document.querySelectorAll('aside img').forEach(i=>i.onclick=()=>{zoom.querySelector('img').src=i.src;zoom.showModal();});document.querySelector('#zoomClose').onclick=()=>zoom.close();
`+h.slice(end);
h=h.replace('render,lid,context,state:', 'render,lid,step,showKnob,state:');
h=h.replace('固定螺栓、扣鎖釋放、編碼器與線束仍需施工','鉸鏈固定與止退已接回；扣鎖釋放、編碼器、線束與車身固定仍需施工');
h=h.replace('quality/console-parent-R4.md','quality/console-parent-R5.md');
h=h.replace('scene.add(assembly.root);','applyGT01HingeFinishes(T,assembly);scene.add(assembly.root);');
h=h.replace('function layers(next=',`Object.assign(partOffsets,{'I03-HF-F03':[0,0,106],'I03-HF-F02':[0,0,123],'I03-HF-F01':[0,0,145],'I03-HF-S01':[0,50,110],'I03-HF-S02':[0,75,110]});function layers(next=`);
h=h.replace('僅構造教學，螺栓規格及維修路徑未驗。','局部鉸鏈開蓋與抽銷已有離散取樣；完整裝拆工序及材料性能仍需驗證。');
const selectStart=h.indexOf('function select(id){'),selectEnd=h.indexOf('\nfunction render()',selectStart);if(selectStart<0||selectEnd<0)throw Error('Console selection consumer changed');
h=h.slice(0,selectStart)+`function select(id){selected=id;partSelector.value=id;layers(mode);const p=assembly.parts.find(p=>p.userData.id===id||p.userData.subPart===id);if(p){const d=p.userData;document.querySelector('#partRef').src=d.hingeCompletion?.shapeReference||d.reference;document.querySelector('#refTitle').textContent=d.id+' · '+p.name;document.querySelector('#info').textContent=p.name+'\\n用途：'+d.purpose+'\\n接收件：'+(d.receiving.ids?.join(', ')||d.receiving.instanceId||d.receiving.subPart)+'\\n'+d.receiving.relationship+'\\n作者裝入方向：'+d.orientation.insertionDirection.map(v=>v.toFixed(2)).join(', ')+'\\n作者包絡：'+d.authorDimensions.size.map(v=>v.toFixed(2)).join(' × ')+' mm'+(d.hingeCompletion?'\\n接收特徵：'+JSON.stringify(d.hingeCompletion.features):'')+'\\n參考採用：'+d.referenceAdoption.adopted+'\\n未採用／未驗：'+d.referenceAdoption.rejected+'\\n生成參考不是實物量測。鉸鏈的指定抽樣請見逐件接合專頁。';}else{document.querySelector('#partRef').src='references/I03-assembly-r4.png';document.querySelector('#refTitle').textContent='鞍座整體參考';document.querySelector('#info').textContent='56個形體／材料區域，包括20個鉸鏈固定與止退件。拖曳旋轉，滾輪縮放；開啟鉸鏈專頁近看接面、盲孔及抽銷順序。';}view(viewName);}`+h.slice(selectEnd);
await fs.writeFile(new URL('GT01-console-assembly.html',out),h);console.log('GT01-console-assembly.html built');
